# Copy to .env for local development (not required — localhost default).
# For a DEPLOYED web app, set VITE_API_BASE_URL to the public API URL at build time:
#   VITE_API_BASE_URL=https://your-api-host.example.com
# VITE_API_BASE_URL=
